# 安全政策

RhineCode 是一个终端 AI 编程助手。它**会在你的机器上执行命令、读写文件、访问网络**，
所以安全对它不是附加项。这份文档讲两件事：**发现漏洞往哪报**，以及**你自己用它时要注意什么**。

英文读者：this project's security policy is maintained in Chinese;
please open a GitHub Security Advisory and write in either language.

---

## 支持的版本

| 版本 | 是否接收安全修复 |
| --- | --- |
| `main` 分支最新提交 | ✅ |
| 已发布的 tag | ⚠️ 目前只有 `0.1.0`（开发中），修复一律推进 `main`，不做回移植 |

项目处于早期阶段，**尚未做正式版本发布**。请始终从 `main` 安装：

```bash
git clone https://github.com/504223641/RhineCode-Agent.git
cd RhineCode-Agent
pip install -e .
```

---

## 报告漏洞

**请不要用公开 Issue 报告安全问题。**

首选渠道：GitHub 的 **Security Advisory**（仓库页 → Security → Report a vulnerability）。
它是私密的，在修复发布之前不会公开。

如果那个入口不可用，请在 Issue 里只写一句「有安全问题需要私下联系」，
不要贴任何细节，维护者会回复联系方式。

### 请在报告里写什么

- 受影响的版本 / commit
- 复现步骤，越具体越好（**尤其欢迎一段能直接跑的最小复现**）
- 你认为的影响面（能读到什么、能写到什么、能执行什么）
- 如果涉及某条安全边界失效，请指明是哪一条——本项目的安全论证逐条写在
  `CLAUDE.md` 的「安全边界」一节与 `README.md` 的同名小节里

### 响应预期

这是一个**个人维护的项目，没有 SLA**。尽力目标：

| 阶段 | 目标 |
| --- | --- |
| 首次回复 | 7 天内 |
| 确认 / 否认 | 14 天内 |
| 修复合入 `main` | 视复杂度，会在 advisory 里同步进度 |
| 公开披露 | 修复合入后，或自报告起 90 天，以先到者为准 |

如果超时没有回复，欢迎在公开 Issue 里催一句「安全报告未回复」（**仍然不要贴细节**）。

### 致谢

修复发布时会在 advisory 与 commit 里致谢报告者，除非你要求匿名。
本项目**没有漏洞赏金**。

---

## 什么算漏洞

本项目的安全模型是：**权限边界由代码强制，边界之内的灰色地带由模型裁量。**
详细论证在 `CLAUDE.md` 的「安全边界」一节。判断一份报告算不算漏洞，看它落在哪一侧。

### ✅ 算漏洞

- 绕过①**危险命令黑名单**（`permission/blacklist.py`）——这一层的性质是
  「不可被任何配置、权限模式或人工确认放开」
- 绕过②**路径沙箱**：让文件工具读到 / 写到工作区之外
  （子 Agent 隔离工作区的边界同理）
- 绕过②′**网络硬校验**：让 `web_fetch` 访问 `file://`、回环地址、内嵌凭据的 URL
- 绕过②″**保护路径**：不经用户确认就改写 `.rhinecode/` 下的配置或 `.git/`
- 绕过③**用户写下的 `deny` 规则**
- 让 **Hook 放宽**权限（`pre_tool_use` 的设计是只能收紧，没有 allow）
- 让**子 Agent 拥有比主对话更大的能力**（工具集、权限档、预授权任一处）
- **明文密钥泄漏**到不该去的地方：写进模型请求、写进未被掩码的 trace 字段、
  传给不该拿到它的子进程
- **提示注入**造成上述任一后果——例如从网页 / 搜索结果 / 队友消息里读到的内容
  导致模型跨过某条硬边界

### ❌ 不算漏洞（已知边界，都记录在案）

这些是**明确接受的取舍**，报告它们不会被当成新发现（但欢迎讨论怎么改进）：

- **`run_command` 跑起来的程序不受路径沙箱与网络策略约束。** 沙箱是应用层的
  前缀校验，管不住子进程自己 `open()` 的文件或自己发的请求。
  OS 级沙箱（Seatbelt / bubblewrap）是已知后续项 #4，而 Windows 是本项目主力平台，
  上游 Claude Code 与 Codex 同样不支持原生 Windows 沙箱。
- **MCP stdio Server 的子进程行为同上。**
- **`hooks.yaml` 里的命令直接执行、不过②③④⑤层。** 那是**你写的配置**，
  不是模型行为——见下方「你自己要注意什么」第 2 条。
- **缺省预设 `auto` 不弹确认面板。** 这是产品选择，不是缺陷。要更严请写 `deny` 规则。
- **C16 分类器会误判。** 它是一个模型，官方公布的同类拦截率是 89%。
  「某条危险命令没被它拦下」不算漏洞——它是第④层的裁量，不是硬边界。
  「某条命令绕过了①②②′②″③」才算。
- **长对话里用户在对话中声明的边界会随上下文压缩失效。** 要硬保证只能写 `deny` 规则。
- **端到端驱动设施（`tests/e2e/`）的控制通道不鉴权。** 它只绑 `127.0.0.1`、
  只在测试宿主活着时存在，是刻意接受的取舍。

---

## ⚠️ 你自己要注意什么

**这一节比上面那节更可能救到你。** 下面五条，每一条都对应「不知道就会中招」的场景。

### 1. 缺省配置不会问你

缺省预设是 `auto`，它的权限档是**放行档**。这意味着：

> 装完直接跑，模型在你的工作区里**写文件、跑命令都不会弹确认面板**。

缺省下仍然会弹面板的只有三类：**写 `.rhinecode/` 下的配置或 `.git/`**、
**访问网络**（未建立域名白名单时）、以及**你自己写的 Hook `ask` 规则**。

**运行期没有「变严」的开关**（`/perm` 已删除）。要更严，唯一手段是在
`permissions.yaml` 里写 `deny` 规则：

```yaml
# <项目根>/.rhinecode/permissions.yaml
deny:
  - "Bash(git push *)"
  - "Write(secrets/**)"
  - "WebSearch"          # ⚠ 必须不带括号，WebSearch(*) 一律不生效且没有任何警告
```

⚠️ **别在不信任的目录里启动它。** 工作目录就是沙箱的边界，
在 `C:\` 或 `~` 下启动等于把整个用户目录交出去。

### 2. 克隆别人的仓库之前，先看 `.rhinecode/hooks.yaml`

**这是本项目最大的攻击面。** Hook 的动作**直接执行**——不经模型、不经确认面板、
不过权限管线的②③④⑤层。`git clone` 一个仓库再在里面启动 `rhine`，
对方写在 `session_start` 上的命令**就在你机器上跑起来了**。

对冲手段只有一个：启动时会**逐条列出**每条项目级 Hook 的「事件 → 动作原文」
（命令串与 URL 完整不截断、橙色醒目、**每次启动都提示**）。**请真的读那几行。**

同一目录下另外两处也随仓库分发，危险程度低一个量级（它们只是「发给模型的文本」，
指挥的每个工具调用照样过完整权限管线），但**仍应与评审代码同等对待**：

- `.rhinecode/skills/` —— 可复用的操作流程，正文可以指挥模型读写文件、执行命令
- `.rhinecode/agents/` —— 可委派的子 Agent 角色定义

用 `/hooks`、`/skills`、`/agents` 查看当前加载了哪些、各自来自哪一层。

### 3. `config.yaml` 里是明文 API Key

- **别提交。** 本仓库的 `.gitignore` 已经忽略它，但**那只在开发 RhineCode 时生效**。
- 想连模型都读不到，写一条 `deny: Read(config.yaml)` 规则
  （grep / glob 的间接泄露也一并挡住）。
- `mcp.yaml` 的 `env` / `headers` 同样可能含密钥，请用 `${VAR}` 引用环境变量，
  不要写真实值。
- 模型经 `run_command` 跑命令时，子进程的环境变量里含 `API_KEY` / `TOKEN` /
  `SECRET` 等字样的条目会被剔除。**但这不覆盖 MCP stdio Server 的子进程**
  （见 `docs/review/04-security.md` 的 S1）。

### 4. `--trace` 的产物比会话存档还敏感

行为记录（`--trace`）会把**完整的模型请求与响应、每次工具执行的参数与输出原文**
写进 JSONL，且**不做任何截断**。模型读过配置文件时，那份内容**逐字全量**落盘，
**其中可能含明文 API Key**。

- 产物落在**启动 `rhine` 的那个项目**根下的 `.rhinecode/traces/`。
  去别的项目跑 trace 前，先给**那个项目**的 `.gitignore` 补上 `.rhinecode/`。
- **勿提交、勿外传、勿贴进 Issue。** 配置快照里的 `api_key` 已被掩码，
  但那只覆盖快照本身，工具输出里的泄漏靠 `.gitignore` 兜底。
- 同级敏感的还有：`.rhinecode/sessions/`（完整对话原文）、
  `.rhinecode/context/`（工具结果存盘）、`.rhinecode/memory/`、
  `.rhinecode/worktrees/`（完整源码 checkout）。这五项都已在本仓库 `.gitignore` 里。

### 5. 网络能力是一条外泄链路

- **`web_fetch` 只取不发**（无请求体、无自定义头、无认证），但它把
  「读文件」与「访问网络」凑齐了：一条「网页里藏一段伪装成系统指令的文本 →
  骗模型读配置 → 把内容拼进下一次抓取的查询参数」的链路因此成立。
  防御是三道叠加：不可信内容标注 + 域名白名单 + 只取不发。
  ⚠ **缺省配置下第二道不存在**——不写任何域名规则时，唯一的实际拦截是每次弹确认。
  想要真边界，必须在**用户级或项目级** `permissions.yaml` 里写下：

  ```yaml
  allow:
    - "WebFetch(domain:github.com)"
    - "WebFetch(domain:*.python.org)"
  ```

  （写在**本地级** `permissions.local.yaml` 里只放行、不建立白名单。）

- **`web_search` 往外发的是你的问题本身。** 查询词原样进第三方服务商的日志，
  而搜索**没有域名白名单这回事**。模型排查报错时很自然就会把项目内部的类名
  拼进查询词——那不是滥用，是正常使用。
  真正的硬边界只有两个：`search.enabled: false`，以及
  `deny: WebSearch`（⚠ **必须不带括号**）。

- **域名策略只管 `web_fetch`。** `run_command` 跑起来的 `curl` / `wget`
  与 MCP 工具都不受它约束。

---

## 加固清单（想更严的话）

```yaml
# <项目根>/.rhinecode/permissions.yaml
deny:
  # 密钥
  - "Read(config.yaml)"
  - "Read(.env)"
  # 不可逆的远端操作
  - "Bash(git push *)"
  # 关掉主动外发（两条都要，且都不带括号）
  - "WebSearch"
  # 关掉「模型自己添加并启动外部 MCP Server」
  - "mcp_add_server"

allow:
  # 建立域名白名单：写下任意一条之后，未列出的域名一律硬拒而不是弹面板
  - "WebFetch(domain:docs.python.org)"
```

```yaml
# ~/.rhinecode/config.yaml
search:
  enabled: false          # 彻底关掉网络搜索
web_fetch_enabled: false  # 彻底关掉网络访问
```

给子 Agent 角色声明更严的档位（`.rhinecode/agents/<名字>.md` 的 frontmatter）：

```yaml
permission_mode: default   # 或 strict；实际生效取「主对话档与本值里更严的那个」
```

⚠️ **别把这份清单当成完整的加固方案。** 它只覆盖有配置开关的那部分；
`run_command` 子进程的行为、MCP Server 子进程的行为都在这些开关之外
（见上面「不算漏洞」一节）。**真正的隔离手段是容器或虚拟机。**

---

## 相关文档

| 文档 | 内容 |
| --- | --- |
| `README.md` 的「安全边界」一节 | 面向用户的完整边界清单（25 条） |
| `CLAUDE.md` 的「安全边界」一节 | 逐条安全论证与理由，含每条的已知局限 |
| `docs/review/04-security.md` | 最近一次安全复审的结论与变异实测记录 |
| `docs/internals/known-issues.md` | 已知后续工程项与各章明确不做的范围 |
