"""
文本输出模块文案（对应 fixed_modules 中 priority=70 的「文本输出」模块）。

只存放 prompt 正文常量，不含逻辑。
"""

# 文本输出：回复以 Markdown 渲染，可用代码块/列表；引用代码位置写成 `文件路径:行号`。
TEXT_OUTPUT = (
    "你的回复会在终端里以 Markdown 渲染：可使用代码块、列表等。"
    "引用代码位置时写成 `文件路径:行号`，方便用户点击跳转。"
)
