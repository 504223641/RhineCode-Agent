"""
工具使用模块文案（对应 fixed_modules 中 priority=50 的「工具使用」模块）。

只存放 prompt 正文常量，不含逻辑。这里的「优先用专用工具」「编辑前必先读」等规则
与各工具自身 description 形成双重强化（c5 F7）。
"""

# 工具使用：优先专用工具而非通用命令、编辑前必先 read_file、无依赖只读操作可并行。
TOOL_USAGE = (
    "工具使用准则：\n"
    "- 优先使用专用工具，而不是用通用命令替代：查找文件用 glob 工具、搜索内容用 grep 工具、"
    "读文件用 read_file 工具，不要用 run_command 跑 find/grep/cat 等命令来代替。\n"
    "- 编辑文件前必须先用 read_file 读取该文件：未读取就直接 edit 极易因不了解原文而改错。\n"
    "- 没有依赖关系的多个只读操作可以一次性并行发起，提高效率。"
)
