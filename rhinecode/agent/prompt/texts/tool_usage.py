"""
工具使用模块文案（对应 fixed_modules 中 priority=50 的「工具使用」模块）。

只存放 prompt 正文常量，不含逻辑。这里的「优先用专用工具」「编辑前必先读」等规则
与各工具自身 description 形成双重强化（c5 F7）。
"""

# 工具使用：优先真实专用工具、编辑前必先 read_file、无依赖只读操作可并行、修改后要验证。
TOOL_USAGE = (
    "工具使用准则：\n"
    "- 优先用专用工具，别用通用命令替代：读文件用 `read_file`，找文件用 `glob_files`，"
    "搜内容用 `grep_content`；只有需要构建、测试、运行脚本或专用工具做不到时，才用 `run_command`。\n"
    "- 编辑文件前必须先用 `read_file` 读过目标区域，理解上下文后再用 `edit_file` 精确修改；"
    "需要新建或整体替换时才用 `write_file`。\n"
    "- 没有依赖关系的多个只读操作可一次并行发起，提高效率；有副作用的操作按顺序执行，并在结果返回后"
    "再决定下一步。\n"
    "- 修改后优先用 `run_command` 执行相关测试、类型检查、构建或最小验证命令；验证失败时先读输出，"
    "定位原因后再修。"
)
