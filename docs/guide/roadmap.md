# 章节文档地图与后续补齐项

> RhineCode 用户手册 · [返回手册目录](README.md) · [返回项目 README](../../README.md)

## 当前阶段文档

**当前主线是 C16（命令与网络的分类器审查），文档在
[`docs/c16/`](../c16/README.md)**——四份 `spec` / `plan` / `task` / `checklist`
+ `README`（导航）+ `acceptance/`。

> ⚠ 读 C16 的验收记录时留意 `acceptance/transcript-scope.md`：它用 944 次直调
> 判定的两栏对照，**推翻了 spec 里「早先消息会污染后续判定」那条成因的一半**，
> 并给出了修法前后的误伤率 / 漏拦率。

C15（子 Agent 协作）文档在 [`docs/c15/`](../c15/README.md)——四份
`spec` / `plan` / `task` / `checklist` + `README`（导航）+ `acceptance/live-model.md`。

> ⚠️ 进门先读 [`docs/c15/README.md`](../c15/README.md) 的「四处漏了不报错的坑」
> 与「开发期发现的三处 spec 没覆盖的问题」。
>
> 更要读 [`acceptance/live-model.md`](../c15/acceptance/live-model.md)：
> 真实模型验收**抓出 6 个产品问题**（全部已修），末尾一张表逐个说明
> 「单测为什么没抓到」。共同点是它们全都属于**机制正确、但用起来不对**——
> 五个只有真实模型自己做选择时才会暴露，一个只有按 checklist 逐条验才会暴露。

C14（子 Agent 工作区隔离）文档在 [`docs/c14/`](../c14/README.md)，
C13（子 Agent 系统）在 [`docs/c13/`](../c13/README.md)。

> ⚠️ 进门先读它的「开发期的两次设计修订」：委派的执行模型（发起与等待分离）与
> 项目级角色的启动提示，都在开发期改过一次。四份文档里带「修订记录」的地方都是
> 它们留下的，`task.md` 整篇是当初的执行计划、刻意未逐条改写。

C12（Hook 系统）文档在 [`docs/c12/`](../c12/README.md)。

> ⚠️ 读 `docs/c12/spec.md` 的 F2 边界第 2 条时**必须连勘误块一起读**：那条原文与
> F6 的管线位置自相矛盾（`pre_tool_use` 排在五层权限管线之前，在跑判定之前无从知道
> 会不会被拒绝）。实现按「本条只管后置事件」执行，`docs/c12/README.md` 有对照表。

C11 的全部文档收在 `docs/c11/` 一个目录下，**进门先读 [`docs/c11/README.md`](../c11/README.md)**——它是导航，也写明了「同一议题两份文档说法不同时以谁为准」。分三块：

- **产品能力（Skill 系统）**：顶层 `spec.md` / `plan.md` / `task.md` / `checklist.md` 是 C11 原始设计，`docs/c11/align/` 是**对齐 Agent Skills 开放标准的改造**。**冲突以 `align/` 为准**——`allowed-tools` 的语义、命令名来源、执行模式字段等五处在改造中反转了。原始那份刻意保留，用于追溯「当初为什么那样设计」。
- **跨阶段测试设施**：`docs/c11/testing/`，含 `brief.md`（需求交底）、`p0-trace/`（行为记录器）、`p1-driver/`（端到端驱动设施）。**不占章节号、不属于 Skill 系统**，服务 C2–C11 与未来所有阶段的验收。
- **验收记录**：`docs/c11/acceptance/` 下四份真实模型实跑报告，每条判据分「机器判到了什么」与「据此做的判断」两栏。

C10（斜杠命令系统）、C9（记忆系统）、C8（上下文管理）、C7（MCP 客户端）、C6（五层防御权限系统）、C5（结构化系统提示与缓存策略）、C4（Agent Loop 与 Plan Mode）文档仍保留，用于追溯设计来源。

**扩展**（不占章节号）的文档在 [`docs/extensions/`](../extensions/README.md)，目前有 9 个：网络访问 `web_fetch`、网络搜索 `web_search`、保护路径层 ②″、auto / plan 两模式、Skill 作者期、主对话待办清单、澄清提问面板、TUI 显示体系、工具活动归并。**下一步做什么**见 [`docs/todo/`](../todo/README.md)，按优先级编号、每份自带可一键复制的开工 Prompt。

## 后续补齐项

> ⚠ **这一节是历史快照，不是当前状态。** 权威清单在
> [`docs/internals/known-issues.md`](../internals/known-issues.md)——那份带编号锚点
> （多处文档按「已知项 #4 / #17 / #18」互指），并逐条记着哪些已修、哪些已被后来的
> 决策推翻。两处冲突时**一律以那份为准**。下面这份保留下来，是因为它记着这些条目
> 当初被提出时的措辞与理由。

以下问题已在工程审查中确认，但不属于当前阶段开发范围，后续章节再统一设计和实现：

1. **委派与组队的触发率**（C15 验收 + 用户实测，口径已于 2026-08-10 反转）：先是观测到**欠触发**（明说「组个队」它一次就做对，但自然场景里从不主动用），据此把三处提示词写成推力；结果**过触发**——用户实测「一个非常简单的任务都要让子 Agent 去做」。⚠ **两次观测出自同一个模型**，所以问题不在模型强弱，在于那些推力是单向的（只写了该委派的理由）。现已整体对齐 Claude Code：**默认不委派，用户开口是主路径，冷启动是贵的路径**，并给了一条可数的下限（一两次工具调用能做完的自己做）。⚠ **尚未做真实模型复测**——这正是待办的全部内容，详见 [`docs/todo/6-delegation-trigger-eval.md`](../todo/6-delegation-trigger-eval.md)。
2. **`Esc` 不会取消正在跑的子 Agent**（C14 真实模型验收实测发现）：按 `Esc` 只停主循环，子 Agent 线程继续跑到底——实测里它在 `Esc` 之后又跑了 7 轮、写文件、提交、留下一个工作区。隔离场景下不至于造成损害（写的都在工作区里），但**非隔离子 Agent 会继续往主项目根里写**。要停它现在得用 `/agents cancel all`。改法牵动 C13「委派永不阻塞」的契约，已于 PR #27 单独决策：**语义保留不改**，只在按下 `Esc` 时明确告知还有几个子 Agent 在后台跑。
3. **`worktree.link` 目前一律降级为 `copy`**：软链指向工作区之外，被权限管线第②层拒绝，子 Agent 一个字节都读不到。已止血为「降级 + 明确警告」，要真正可用得动第②层边界判定——**2026-08-09 用户拍板不做**（没有真实的大目录共享需求，而代价是改 `path_guard` 的判定函数签名并动权限管线第②层）。理由留在 [`docs/todo/README.md`](../todo/README.md) 的「已取消」一节。
4. 子 Agent 工作区隔离后续项：主对话自身进出隔离工作区、工作区之间的合并策略、后台定时清理与手动清理入口、把主项目根的未提交改动带入工作区、由系统代替子 Agent 提交、环境初始化的启发式自动识别、按「已合并」判定删除分支、非 Git 版本控制系统的隔离。另有三条已知边界与 OS 级沙箱同源：MCP 工具不受工作目录隔离约束、`run_command` 子进程内部的路径访问不受约束、隔离工作区里没有 `.rhinecode/`（不影响项目级配置生效，它们在装配期就已加载进内存）。
5. Hook 系统后续项：子 Agent 动作的真实运行（现为占位）、`once` 的持久化、执行顺序的显式优先级、迭代级事件、配置中的字符串插值、HTTP 动作参与拦截决策、`/hooks reload` 热更新、本地级 `hooks.yaml`、在 Skill frontmatter 里声明 Hook、Hook 修改工具参数或工具结果。
6. Skill 系统：市场分发与版本管理、嵌套激活、参数 schema、模板引擎、并行执行、跨会话保持激活态、文件监听式自动热更新（当前需显式 `/skills reload`）。
7. API Key 与敏感配置的读取脱敏、环境变量化或工作区外管理。
8. `write_file` / `edit_file` 的文件系统级原子写入。
9. OS 级沙箱（Seatbelt / bubblewrap），约束 `run_command` 子进程自身发起的文件/网络访问。
10. 权限系统后续项：资源配额、审计日志（**网络请求限制已由 web_fetch 扩展兑现**）。
11. 开发环境依赖固定与 CI，让 `compileall` / `unittest` 在标准环境稳定运行。
12. MCP 后续项：Server 健康检查与自动重连、资源 / 提示词 / 采样等非工具能力、MCP 工具的细粒度权限映射与执行超时可配置化。
13. 上下文管理后续项：精确 tokenizer（当前仅近似估算）、摘要策略的质量/机器学习优化、存盘文件的清理与生命周期、除窗口大小外其它阈值的可配置化、跨会话摘要持久化。
14. 行为记录后续项：记录文件的自动清理与轮转、实时流式查看、可视化时间线、跨运行对比、截断阈值可配置化、采样与按类型开关（当前只有全开与全关两态）。（TUI 驱动器已实现，见 `tests/e2e/`。）
15. 子 Agent 协作后续项：跨机器/分布式团队、成员间实时流式通信、队员之间互相委派、看板与花名册的跨会话持久化、更复杂的依赖约束（优先级/时限/子任务树）、消息的已读回执与附件、消息级与任务级的新 Hook 事件、队员的人在回路。另有一条实现期定下的边界：**隔离委派与待命互斥**（工作区何时结算没有第二个说得通的答案）。
16. 记忆系统后续项：向量数据库/RAG 语义检索、团队记忆同步/跨机器共享、跨实例实时一致性、记忆自动清理与遗忘机制、各阈值可配置化、存档格式版本迁移与加密存储。

