# 扩展文档

**这里放的是「工具/能力扩展」，不占章节号。**

`docs/c2` … `docs/c11` 是**主线章节**——每一章引入一个新的能力层级（Agent Loop、权限系统、
MCP 客户端、上下文管理、记忆系统、斜杠命令、Skill 系统），架构表里会多一层，
`CLAUDE.md` 的能力表里会多一行。

本目录放的是**不构成新层级的增量**：在既有层上加一个工具、给既有机制加一种规则、
扩一个已有能力的边界。它们同样走完整 `/spec` 流程（spec → plan → task → checklist），
同样要审批、要验收，只是**不编章节号**。

## 目录约定

```
docs/extensions/
├── README.md              ← 你在这里
└── <扩展名>/              ← 一个扩展一个目录，短横线命名
    ├── spec.md            ← 做什么
    ├── plan.md            ← 怎么做
    ├── task.md            ← 按什么顺序做
    └── checklist.md       ← 做对了没
```

需要留验收记录时，在该扩展目录下加 `acceptance.md` 或 `acceptance/`，
口径与 `docs/c11/acceptance/` 一致（每条判据分「机器判到了什么」与「据此做的判断」两栏）。

## 怎么判断该进这里还是开新章节

| 情况 | 去处 |
| --- | --- |
| 引入一个新的能力层级，架构表要多一层 | 新章节 `docs/cN/` |
| 在既有层上加工具、加规则、扩边界 | 本目录 |
| 跨阶段的测试设施（不属于任何产品能力） | 随其服务的章节走，如 `docs/c11/testing/` |
| 只是「下一步可能做什么」的候选项 | `docs/todo/` |

拿不准时看一条：**`CLAUDE.md` 的能力表要不要多一行？** 要，就是章节；不要，就是扩展。

## 当前扩展

| 扩展 | 状态 | 一句话 |
| --- | --- | --- |
| [`web-fetch/`](web-fetch/spec.md) | **已实现并验收**（2026-07-29；31 任务 / 1165 项测试全绿 / 6 个端到端场景真实模型各 3 轮 6/6 / [验收记录](web-fetch/acceptance.md)） | 网络访问工具。RhineCode 第一个能主动向外发数据的工具，因此同时建立应用层网络边界（域名策略 + 结构性硬校验），并把抓回的内容当作不可信输入对待 |
| [`tui-display/`](tui-display/spec.md) | **已实现并验收**（2026-08-11；44 任务 / 2504 项测试全绿 / 32 条 AC 逐条对照 / [验收记录](tui-display/acceptance.md)） | TUI 显示体系对齐 Claude Code。起点是一个具体症状——**子 Agent 在工作时界面上没有任何状态**（派出去之后几分钟静默，唯一的活体信号是状态栏角落那个计数），最后做成八组：子 Agent 活动区（主线程轮询、三个数字实时跳、`Ctrl+O` 展开内部调用、终态留痕与历史区同源）、工具行主参数（`Read(app.py)` 取代 `Read(path=app.py)`）、命令报告分级、系统行四级、面板序号与数字键直选、去表情与连按两次 `Ctrl+C` 退出、「笔记」统一为「记忆」、历史区贴顶与长输出折叠 |
| [`tui-activity-fold/`](tui-activity-fold/spec.md) | **已实现并验收**（2026-08-13；38 任务 / [验收记录](tui-activity-fold/acceptance.md)）。⚠ **真机复核跑出 22 条问题，而单元测试事前抓到 0 条**——本扩展最值得读的是那份验收记录，不是 spec | 工具活动归并与回合状态行。起点是一个具体症状——**用户让它「读一下项目」，十几次 Read/Glob/Grep 全部平铺在聊天窗口里，把结论淹掉了**（20 次调用 = 净增 40 行且全部累积）。三组：历史区批次归并（连续的**只读检索**调用收成一行聚合语，写文件/跑命令/委派一律独立成行——**被折叠的永远只是「读」**）、三级信息密度（`Ctrl+O` 折叠 → 逐条 → 全文循环，当前档位与下一档写在屏幕上）、底部回合状态行（spinner + 本回合耗时 + token + `esc 中断`，顺带把「每个工具行一个定时器」收敛成全局一个） |
| [`protected-paths/`](protected-paths/spec.md) | **已实现并验收**（2026-08-14；14 任务 / 2716 项测试全绿 / 61 项单测 + 6 个脚本化端到端场景 + **6 个场景真实模型复跑** / **8 种错法逐个变异验过判据真的会红** / [验收记录](protected-paths/acceptance.md)） | 保护路径层 ②″。起点是一个具体缺口——**模型能改写自己的权限配置**（`.rhinecode/` 下那批文件决定「以后会发生什么」，而它们的写入在②沙箱看来完全合法、③规则不表态、④放行档直接 ALLOW）。做法是在 `engine.decide` 出口加一个**收紧器**：命中保护路径且结论非 DENY 时升级为 ASK，判定基准是 `request.cwd` 因此 C14 的隔离子 Agent 天然不受影响。确认面板在该场景下**去掉「永久放行」**——那个选项写的是③层规则，而本层不被③层消解，留着它就是一个点了没用的按钮 |
| [`skill-authoring/`](skill-authoring/spec.md) | **已实现并验收**（2026-07-30；22 任务 + R 系列 5 项 / 1241 项测试全绿 / 离线 2 场景 + **真实模型 7 场景各 3 轮共 21 轮全通过** / [验收记录](skill-authoring/acceptance.md)） | Skill 作者期。对齐改造让 Skill **可导入**，这个扩展让它**可创作**：`/skills` 体检产出**可操作建议**（八项检查，纯函数零 IO，「建议改成 xxx」而非「警告：xxx」）+ 内置 `skill-creator` 样板（创作 / 适配 / 按建议修复三用途）。缺口由真实使用暴露——此前 C11 的 43/43 判据全部建立在「已经有一份写好的 Skill」这个前提上 |
| [`todo-list/`](todo-list/spec.md) | **已实现**（2026-08-18；26 任务 / 114 项专项测试 / 布局几何两轮实测（复刻品 + 产品代码）四条判据全中 / 真机复核待做） | 主对话的待办清单。起点是一个具体症状——**一个十几步的任务跑起来，用户只能从工具行反推进度**，而 tui-activity-fold 把只读检索调用归并成一行之后这件事更看不出来了（归并压掉「做了什么」，待办回答「还剩什么」）。三个设计决定各有论证：**新起数据结构**（C15 那份为多 Agent 抢活而生，认领人与依赖在这里恒空）、**整表覆写**（C15 用增量是因为并发写入方会 lost update，主对话没这个约束）、**钉在历史区内部底端占位**（条数可变故固定占位不成立，按需 display 又会每次覆写都抖）|
