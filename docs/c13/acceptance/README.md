# C13 验收记录

按 [`../checklist.md`](../checklist.md) 逐条执行的实跑结果。

## 记录格式

每条判据分**两栏**，照 C11 `docs/c11/acceptance/` 的先例：

| 栏 | 写什么 |
| --- | --- |
| **机器判到了什么** | 命令输出、trace 事件、断言结果——**可复现的事实** |
| **据此做的判断** | 从那些事实推出的结论 |

分两栏是因为这两件事经常被混在一起写，而它们的可信度完全不同：
前者可以重跑验证，后者是人的推断。混在一起时，一个错误的推断会伪装成实测结论。

## 待填

| 文件 | 内容 | 状态 |
| --- | --- | --- |
| [`automated.md`](automated.md) | 🤖 那 50 条（单元测试 + 真实装配链路） | **✅ 2026-08-07 全部通过** |
| [`live.md`](live.md) | 👤 真实模型（deepseek-v4-flash）跑的场景 | **✅ 2026-08-07 通过；首跑抓出一个结构性缺陷，已修** |

真实模型那轮已覆盖 AC6b / 场景 3 / 场景 4 / 场景 7 / 场景 8 与 AC19，
**并抓出一个 20 项单元测试都没抓到的结构性缺陷**（分支式父快照未做不成对清理，
`fafeef7` 已修）。

**剩余仍需人眼的三条**：

1. **AC6b / 场景 7** —— 模型会不会**主动**委派（只有真实模型能答；真实模型那轮
   已观察到它主动委派，但这类偏差值得多跑几次）
2. **AC25e** —— 连续跑 10 分钟后界面无卡顿、无残留「运行中」行
3. **场景 8** —— `explorer` 结论的质量（自包含、可直接用）

另有 **AC18（并发上限）** 真实模型下不易稳定构造 4 次并发委派，已由单元测试覆盖。

> 两条原有的人眼判据已随开发期修订消失：**AC4** 的启动提示醒目程度
> （提示本身已移除，信息改由 `/agents` 承载）与 **AC17c** 的 `Ctrl+B` 按键手感
> （该键已删除）。来龙去脉见 [`../README.md`](../README.md)。

具体步骤见 [`manual.md`](manual.md)。

其余全部由 Claude 用单元测试与脚本模式驱动设施跑完，不花 API 费用。
