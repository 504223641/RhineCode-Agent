# 配置文件（含 API key，禁止提交）
config.yaml

# Python 构建产物
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# 虚拟环境
.venv/
venv/
env/
TemText.txt

# c5 缓存命中调试日志（运行时生成，用于验证缓存策略）
.rhinecode_debug.log

# c6 本地级权限配置（含「永久放行」自动追加的规则，按机器/个人，不提交）
**/.rhinecode/*.local.yaml
