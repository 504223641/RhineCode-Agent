# 配置文件（含 API key，禁止提交）
config.yaml

# Python 构建产物
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# 虚拟环境
.venv/
venv/
env/
TemText.txt

# c5 缓存命中调试日志（运行时生成，用于验证缓存策略）
.rhinecode_debug.log

# c6 本地级权限配置（含「永久放行」自动追加的规则，按机器/个人，不提交）
# c7 MCP 本地级配置同样按此模式忽略（如需机器私有的 mcp.local.yaml）。
# 注：项目级 .rhinecode/mcp.yaml 是「随仓库走、可提交」的（密钥用 ${VAR} 引用、不落盘），故不忽略；
#     用户级 ~/.rhinecode/mcp.yaml 在仓库外，天然不会被提交。
**/.rhinecode/*.local.yaml

# c8/c9 运行时数据（不提交）：工具结果存盘、会话存档、自动笔记与锁文件
# 都可能包含对话原文与被读过的敏感文件片段（含 config 等），必须留在本机。
# 注：项目级 .rhinecode/RHINE.md、permissions.yaml、mcp.yaml、hooks.yaml 是「随仓库走、可提交」的，不受影响。
#     ⚠ hooks.yaml 里的命令会**直接执行**（不经模型、不经确认面板），评审它要与评审代码同等对待。
#     项目级 .rhinecode/skills/ 同样随仓库提交、团队共享（c11）——它们是团队约定的
#     可复用操作流程。注意其正文可以指挥模型读写文件与执行命令，评审时应与代码同等对待。
**/.rhinecode/context/
**/.rhinecode/sessions/
**/.rhinecode/memory/
# trace 行为记录（测试设施）：与会话存档同属敏感产物——里面有被读过的**文件内容**
# 与命令输出，模型读过配置文件时还可能含**明文 API Key**。必须留在本机、勿外传。
# `**/` 前缀与上面三行同口径，否则嵌套项目路径下失效。
**/.rhinecode/traces/
# C5 运行日志：敏感度**远低于** trace（只记「哪一步、什么结果、耗时多少」，
# 刻意不记消息正文、工具参数与工具输出），但它仍然会写下文件名、工具名与
# 权限判定结果，属本机产物、没有提交的理由。`**/` 前缀与上面几行同口径。
**/.rhinecode/logs/

# c14 子 Agent 隔离工作区：每个都是一份**完整的源码 checkout**，
# 且环境初始化可能把本地配置（含密钥）复制进去。与 context/sessions/memory/traces
# 同口径——`**/` 前缀不可省，否则嵌套项目路径下失效。
**/.rhinecode/worktrees/

# ── 开发者本机产物（E6）────────────────────────────────────────────────
# 开源之后贡献者用的编辑器与工具链各不相同，而这些目录**只对生成它的那台机器
# 有意义**：一个人提交了 .vscode/settings.json，下一个人的编辑器就会莫名其妙
# 按别人的偏好行事，且这类文件在 PR diff 里全是噪音。
#
# ⚠ 这里刻意只忽略、**不删除**任何已存在的文件——本仓库当前一个都没有
# （加这几行时验过），所以它是纯预防性的。
.vscode/
.idea/
.DS_Store

# 覆盖率与静态检查的缓存（跑一次就会生成，纯本机产物）
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
